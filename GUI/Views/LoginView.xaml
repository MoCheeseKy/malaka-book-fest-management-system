<UserControl x:Class="GUI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Border Style="{StaticResource CardStyle}" Width="450" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="OPERATIVE LOGIN" FontSize="28" FontWeight="Black" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                <TextBlock Text="Enter your credentials to access the S.E.E.S. Database" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,30" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>

                <TextBlock Text="Email Address" FontWeight="Bold" Margin="0,0,0,5" Foreground="{StaticResource AccentBrush}"/>
                <TextBox x:Name="TxtEmail" Margin="0,0,0,20"/>

                <TextBlock Text="Password" FontWeight="Bold" Margin="0,0,0,5" Foreground="{StaticResource AccentBrush}"/>
                <!-- Using PasswordBox for real password masking, applying basic P3R colors directly -->
                <PasswordBox x:Name="TxtPassword" Margin="0,0,0,30" FontFamily="{StaticResource HarmonyOS}" Background="#0A001D4A" Foreground="{StaticResource TextBrush}" BorderBrush="{StaticResource PanelLightBrush}" BorderThickness="0,0,0,2" Padding="12,12" FontSize="16" CaretBrush="{StaticResource AccentBrush}"/>

                <!-- Slanted Action Button -->
                <Button Content="LOGIN" Margin="0,0,0,15" Click="LoginButton_Click" />

                <!-- Secondary Link -->
                <Button Content="REGISTER NEW OPERATIVE" Style="{StaticResource SecondaryButton}" Click="RegisterButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
