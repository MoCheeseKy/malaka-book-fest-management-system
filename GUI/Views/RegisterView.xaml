<UserControl x:Class="GUI.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <Grid>
        <Border Style="{StaticResource CardStyle}" Width="450" HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="NEW OPERATIVE" FontSize="28" FontWeight="Black" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,5" HorizontalAlignment="Center"/>
                <TextBlock Text="Register a new S.E.E.S. Administrator account" Foreground="{StaticResource MutedTextBrush}" Margin="0,0,0,20" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>

                <TextBlock Text="Full Name" FontWeight="Bold" Margin="0,0,0,5" Foreground="{StaticResource AccentBrush}"/>
                <TextBox x:Name="TxtFullName" Margin="0,0,0,15"/>

                <TextBlock Text="Email Address" FontWeight="Bold" Margin="0,0,0,5" Foreground="{StaticResource AccentBrush}"/>
                <TextBox x:Name="TxtEmail" Margin="0,0,0,15"/>

                <TextBlock Text="Password" FontWeight="Bold" Margin="0,0,0,5" Foreground="{StaticResource AccentBrush}"/>
                <PasswordBox x:Name="TxtPassword" Margin="0,0,0,15" FontFamily="{StaticResource HarmonyOS}" Background="#0A001D4A" Foreground="{StaticResource TextBrush}" BorderBrush="{StaticResource PanelLightBrush}" BorderThickness="0,0,0,2" Padding="12,12" FontSize="16" CaretBrush="{StaticResource AccentBrush}"/>

                <TextBlock Text="Role" FontWeight="Bold" Margin="0,0,0,5" Foreground="{StaticResource AccentBrush}"/>
                <ComboBox x:Name="CmbRole" Margin="0,0,0,25">
                    <ComboBoxItem Content="admin" IsSelected="True"/>
                    <ComboBoxItem Content="user"/>
                </ComboBox>

                <!-- Slanted Action Button -->
                <Button Content="REGISTER" Margin="0,0,0,15" Click="RegisterButton_Click" />

                <!-- Secondary Link -->
                <Button Content="BACK TO LOGIN" Style="{StaticResource SecondaryButton}" Click="BackToLoginButton_Click" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
