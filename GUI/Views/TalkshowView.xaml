<UserControl x:Class="GUI.Views.TalkshowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <!-- MAIN VIEW -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="TALKSHOW MANAGEMENT" FontSize="26" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,5"/>
                    <TextBlock Text="Kelola jadwal talkshow, pembicara, dan kapasitas penonton." Foreground="{StaticResource MutedTextBrush}"/>
                </StackPanel>
                <Button Grid.Column="1" Content="+ ADD NEW TALKSHOW" Click="AddTalkshow_Click" VerticalAlignment="Center" Padding="25,12" />
            </Grid>

            <!-- DATA TABLE -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <DataGrid x:Name="TalkshowDataGrid" SelectionChanged="TalkshowDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Speaker" Binding="{Binding SpeakerName}" Width="150" />
                        <DataGridTextColumn Header="Venue" Binding="{Binding Venue}" Width="120" />
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100" />
                        <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat=\{0:dd/MM/yy HH:mm\}}" Width="130" />
                        <DataGridTemplateColumn Header="Actions" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="ADVANCE STATUS" Style="{StaticResource ActionButton}" Click="AdvanceStatus_Click" Tag="{Binding}" />
                                        <Button Content="EDIT" Style="{StaticResource ActionButton}" Click="EditTalkshow_Click" Tag="{Binding}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- OVERLAY MODAL -->
        <Grid x:Name="ModalOverlay" Background="#A0000000" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Style="{StaticResource CardStyle}" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30">
                <StackPanel>
                    <TextBlock x:Name="ModalTitle" Text="ADD TALKSHOW" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SecondaryAccentBrush}" Margin="0,0,0,20"/>

                    <TextBlock Text="Talkshow Title" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBox x:Name="TxtTitle" Margin="0,0,0,15" />

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Speaker Name" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBox x:Name="TxtSpeakerName" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Venue" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBox x:Name="TxtVenue" />
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Speaker Bio" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBox x:Name="TxtSpeakerBio" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15" />

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Start Time (yyyy-MM-dd HH:mm)" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBox x:Name="TxtStartTime" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="End Time (yyyy-MM-dd HH:mm)" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBox x:Name="TxtEndTime" />
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Max Capacity" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBox x:Name="TxtMaxCapacity" Margin="0,0,0,25" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="CANCEL" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="CloseModal_Click" />
                        <Button x:Name="BtnSaveModal" Grid.Column="1" Content="SAVE TALKSHOW" Margin="10,0,0,0" Click="SaveButton_Click" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
