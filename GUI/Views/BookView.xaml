<UserControl x:Class="GUI.Views.BookView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <!-- MAIN VIEW -->
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="BOOKS MANAGEMENT" FontSize="26" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,5"/>
                    <TextBlock Text="Kelola daftar buku yang tersedia pada suatu Booth spesifik." Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBlock x:Name="TxtCurrentBoothId" Foreground="{StaticResource SecondaryAccentBrush}" Margin="0,5,0,0" FontWeight="Bold"/>
                </StackPanel>
                <Button Grid.Column="1" Content="+ ADD NEW BOOK" Click="AddBook_Click" VerticalAlignment="Center" Padding="25,12" />
            </Grid>

            <!-- DATA TABLE -->
            <Border Grid.Row="1" Style="{StaticResource CardStyle}">
                <DataGrid x:Name="BookDataGrid" SelectionChanged="BookDataGrid_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*" />
                        <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="150" />
                        <DataGridTextColumn Header="ISBN" Binding="{Binding Isbn}" Width="120" />
                        <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100" />
                        <DataGridTextColumn Header="Stock" Binding="{Binding Stock}" Width="80" />
                        <DataGridTemplateColumn Header="Actions" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="EDIT" Style="{StaticResource ActionButton}" Click="EditBook_Click" Tag="{Binding}" />
                                        <Button Content="DELETE" Style="{StaticResource ActionDangerButton}" Click="DeleteButton_Click" Tag="{Binding}" />
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- OVERLAY MODAL -->
        <Grid x:Name="ModalOverlay" Background="#A0000000" Visibility="Collapsed" Panel.ZIndex="100">
            <Border Style="{StaticResource CardStyle}" Width="500" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="30">
                <StackPanel>
                    <TextBlock x:Name="ModalTitle" Text="ADD BOOK" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SecondaryAccentBrush}" Margin="0,0,0,20"/>

                    <TextBlock Text="Book Title" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBox x:Name="TxtTitle" Margin="0,0,0,15" />

                    <TextBlock Text="Author" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBox x:Name="TxtAuthor" Margin="0,0,0,15" />

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ISBN" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBox x:Name="TxtIsbn" />
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Price" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                            <TextBox x:Name="TxtPrice" />
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Stock" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBox x:Name="TxtStock" Margin="0,0,0,15" />

                    <TextBlock Text="Cover Image URL" Margin="0,0,0,5" Foreground="{StaticResource MutedTextBrush}"/>
                    <TextBox x:Name="TxtCoverUrl" Margin="0,0,0,25" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="CANCEL" Style="{StaticResource SecondaryButton}" Margin="0,0,10,0" Click="CloseModal_Click" />
                        <Button x:Name="BtnSaveModal" Grid.Column="1" Content="SAVE BOOK" Margin="10,0,0,0" Click="SaveButton_Click" />
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
