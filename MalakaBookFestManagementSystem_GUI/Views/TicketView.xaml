<UserControl x:Class="MalakaBookFestManagementSystem_GUI.Views.TicketView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MalakaBookFestManagementSystem_GUI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="TICKET PRICES" FontSize="32" Foreground="{StaticResource P3Cyan}" FontFamily="Segoe UI Black"/>
            <Button Content="RELOAD" Command="{Binding LoadTicketPricesCommand}" Style="{StaticResource P3ButtonStyle}" Margin="20,0,0,0" Padding="15,8"/>
            <TextBlock Text="Loading data..." Margin="15,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource P3White}"
                       Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding TicketPrices}" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,20,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ticket Type" Binding="{Binding Type}" Width="*"/>
                <DataGridTextColumn Header="Price (IDR)" Binding="{Binding Price}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
