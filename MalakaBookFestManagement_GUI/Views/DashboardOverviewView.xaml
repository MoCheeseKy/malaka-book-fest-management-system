<UserControl x:Class="MalakaBookFestManagement_GUI.Views.DashboardOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title & Welcome -->
            <RowDefinition Height="Auto"/> <!-- Stats Cards -->
            <RowDefinition Height="*"/>    <!-- Welcome message / decorative area -->
        </Grid.RowDefinitions>

        <!-- Loader status -->
        <ProgressBar Style="{StaticResource P3RProgressBarStyle}" IsIndeterminate="True" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     VerticalAlignment="Top" Margin="0,-15,0,15"/>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="OVERVIEW" Foreground="White" FontSize="26" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBlock Text="Quick metrics overview of Malaka Book Fest 2026." Foreground="{StaticResource SecondaryTextBrush}" FontSize="12"/>
        </StackPanel>

        <!-- Stats Cards Grid -->
        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Booths Card -->
            <Border Grid.Column="0" Background="#CC0A192F" BorderBrush="#1A2F4C" BorderThickness="1" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000" BlurRadius="10" Opacity="0.3" ShadowDepth="3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="ACTIVE BOOTHS" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" FontWeight="Bold" />
                    <TextBlock Text="{Binding TotalBooths}" Foreground="{StaticResource AccentBrush}" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Books Card -->
            <Border Grid.Column="2" Background="#CC0A192F" BorderBrush="#1A2F4C" BorderThickness="1" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000" BlurRadius="10" Opacity="0.3" ShadowDepth="3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="BOOKS IN CATALOG" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" FontWeight="Bold" />
                    <TextBlock Text="{Binding TotalBooks}" Foreground="{StaticResource AccentBrush}" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Talkshows Card -->
            <Border Grid.Column="4" Background="#CC0A192F" BorderBrush="#1A2F4C" BorderThickness="1" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000" BlurRadius="10" Opacity="0.3" ShadowDepth="3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="TALKSHOWS SCHEDULED" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" FontWeight="Bold" />
                    <TextBlock Text="{Binding TotalTalkshows}" Foreground="{StaticResource AccentBrush}" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Tickets/Users Card -->
            <Border Grid.Column="6" Background="#CC0A192F" BorderBrush="#1A2F4C" BorderThickness="1" CornerRadius="8" Padding="20">
                <Border.Effect>
                    <DropShadowEffect Color="#000" BlurRadius="10" Opacity="0.3" ShadowDepth="3"/>
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="MY REGISTERED TICKETS" Foreground="{StaticResource SecondaryTextBrush}" FontSize="10" FontWeight="Bold"  Name="TicketCardHeader"/>
                    <TextBlock Text="{Binding TotalTickets}" Foreground="{StaticResource AccentBrush}" FontSize="36" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Main Banner -->
        <Border Grid.Row="2" Background="#0C1D36" BorderBrush="#1A2F4C" BorderThickness="1" CornerRadius="8" Padding="30" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="WELCOME TO THE FESTIVAL MANAGEMENT" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                <TextBlock Text="You are currently signed in to the Malaka Book Fest desktop client. Use the sidebar on the left to manage books, purchase entry tickets, book seats for live talkshows with guest speakers, or track active publisher booths." 
                           Foreground="{StaticResource SecondaryTextBrush}" FontSize="13" LineHeight="20" TextWrapping="Wrap"/>
                
                <Border Height="1" Background="#1A2F4C" Margin="0,20,0,20"/>
                
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Signed in as: " Foreground="{StaticResource SecondaryTextBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding UserFullName}" Foreground="White" FontSize="12" FontWeight="Bold" Margin="5,0,15,0"/>
                    <TextBlock Text="Role privilege: " Foreground="{StaticResource SecondaryTextBrush}" FontSize="12"/>
                    <TextBlock Text="{Binding UserRole}" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="Bold" Margin="5,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
